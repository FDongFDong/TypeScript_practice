// let MyName: string = null;
// strictNullChecks가 true로 되어 있어 에러 처리

// let u: undefined = null;
let v: void = undefined;

let union: string | null = null; // 합집합처럼 사용할 수 있다. -> union
union = 'FDong';
